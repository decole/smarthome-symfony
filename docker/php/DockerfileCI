# see .gitlab-ci.yml - build DockerfileCommands as commands-test docker image
FROM commands-test

MAINTAINER decole <decole2014@yandex.ru>

ARG USER=gitlab-runner
ARG UID=1001

RUN chmod 777 -R /var/www \
    && && mkdir -p /home/${USER}  \
    && chown -R ${USER}:${USER} /var/www

WORKDIR /var/www

USER ${USER}
